[package]
name = "tiny4linux_assets"
version = "1.0.0"
edition = "2024"

include = [
    "Cargo.toml",
    "src/**",
    "generated/**"
]

[dependencies]
iced = {version = "0.13.1", default-features = false, features = ["image"]}
once_cell = "1.21.3"
